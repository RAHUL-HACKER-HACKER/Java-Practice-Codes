package javaPrograms;
import java.util.*;
import java.lang.*;
public class MathJava {
	public static void main(String[] args) {
		double pi=Math.PI;
		double e=Math.E;
		
		double abs=Math.abs(-200);
		double sqrt=Math.sqrt(25);
		double cbrt=Math.cbrt(125);
		double pow=Math.pow(5, 2);
		double p1=Math.pow(25, 0.5);
		double p2=Math.pow(25, (double)1/2);
		double p3=Math.pow(25, 1d/2);
		double p4=Math.pow(25, 1.0/2);
		
		double radian=Math.toRadians(180);
		double degree=Math.toDegrees(pi);
		double log=Math.log(e);
		double log10=Math.log10(100);
		double randomvalue=Math.random();
		double round=Math.round(10.34);
		double ceil=Math.ceil(23.34);
		double floor=Math.floor(23.34);
		double exp=Math.exp(200);
		
		
		
		double max=Integer.MAX_VALUE;
		double min=Integer.MIN_VALUE;
		double pinfinity=Double.POSITIVE_INFINITY;
		double ninfinity=Double.NEGATIVE_INFINITY;
		
		
		
		double x=(double)5/2+(double)5/2;
		System.out.println(x);
		
		
		

	}

}
